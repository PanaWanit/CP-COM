#include<bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false), cin.tie(nullptr);
	double a,b;
	cin >> a >> b;
	cout << setprecision(6) << fixed << sqrt(a*a+b*b) << "\n";
	return 0;
}

