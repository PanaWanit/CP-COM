#include<bits/stdc++.h>
using namespace std;

int main() {
	ios::sync_with_stdio(false), cin.tie(nullptr);
	double a;
	cin >> a;
	cout << setprecision(6) << fixed << acos(-1)*a*a << "\n" << a*a*2 << "\n";
	return 0;
}

