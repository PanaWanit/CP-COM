#include<bits/stdc++.h>
using namespace std;
const int mxN = 55;
int dp[ ][mxN][mxN];
int main() {
	ios::sync_with_stdio(false), cin.tie(nullptr);
	int l,n;;
	cin >> l >> n;
	for(int i=1; i<=n; ++i)
		cin >> a[i];

	return 0;
}

